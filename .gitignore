# Python
.venv/
__pycache__/
*.pyc

# Piper binary + voice models (240MB+; re-downloaded by scripts/setup_piper.sh,
# cached in CI via actions/cache)
piper/

# Local env overrides
.env

# macOS
.DS_Store

# Generated site (episodes, feed, index) - lives on the `pages` branch as a
# squashed/orphan commit each publish, never on `main`. See
# scripts/fetch_pages_branch.sh and scripts/publish_pages_branch.sh.
docs/

# Scratch space (e.g. voice comparison samples)
_tmp/
